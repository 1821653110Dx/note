/* import modules : stdio.h, stdilb.h */

// def main
{
	// def enum_class : name = color, members = [red, green, blue], value = [1..3]

	/* def enum_var favorate_color: value = null, calss = color */

	/* echo hint. let user input a line of elements, save it */
	// printf"please input your favorate color: (1 .red, 2. green, 3. blue) : "
	// let user input, save it: to = 'favotate_color', format = %u
	
	/* output results */
	// switch next step accoding to 'favorite_color'
	{
		// if = 'red'
			// print"you favorate_color is red"
			// next step
		// if = 'green'
			// print"your favorate_color is green"
			// next step
		// if = 'blue'
			// print"your favorate_color is blue"
			// next step
		// otherwise
			// print "your haven' selected your favorate_color"
	}

	// exit
}
