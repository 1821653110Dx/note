# generate 10 random integers
``` python
import random
random.seed(123)
for i in range(10):
	print(random.randint(1,999), end=',')
```
## explination
```txt
# import random
import random module

# random.seed(123)
> Initialize the random function with seed value 123 to ensure that the random number sequence generated each time the code runs is the same

# for i in range(10):
define the value of i ranges from 0 to '10 - 1'

# print(random.randint(1,999), end=',')
~ = print random.randint(1,999) + ,
	random.randint(1,999) = an integer of random number sequence, which 1 <= and <= 999
```
