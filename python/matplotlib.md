# simple stright line
```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(-1, 1, 50)	# x ranges from -1 to 1 and has 50 values
y = 2*x + 1	# define function 'y' as '2*x +1'

plt.plot(x, y)	# draw an image of the function 'y' with 'x' as a variable
plot.show()	# display img

```

