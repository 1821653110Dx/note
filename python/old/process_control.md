# if
```python
x = int(input('please enter an interger: '))	# display ~, x = the integer that you input

if X < 0 :
	x = 0
	print('Negative changed to zero')
elif x == 0 :
	print('Zero')
elif x == 1 :
	print('Single')
else :
	print('More')

```
# for
```python
words = ['cat', 'window', 'defenestrate']

for w in words :	# run the cycle 3 times
	print(w, len(w))

```
