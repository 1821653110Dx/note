# Create Classes
```python
class Point :	# custom a class called 'Point'
	def draw(self) :	# def a function : draw(self), draw is a method of Point
		print('draw')	

a = Point()	# the class of a is Point
a.draw()		# a.draw = get the value of draw(self)
```
